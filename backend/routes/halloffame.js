const express = require("express");
const router = express.Router();
const db = require("../db");
const auth = require("../middleware/auth");

// gebe alle Nutzer aus die in der db sind basirend nach ihren rank zeige dazu Profilebild Rahmen Namen Rank and
// Route http://localhost:9000/api/halloffame

// Ermittel alle Nutzer aus der db


module.exports = router;

